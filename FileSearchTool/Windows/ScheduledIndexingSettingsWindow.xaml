<Window x:Class="FileSearchTool.Windows.ScheduledIndexingSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="定时索引设置"
        Height="180"
        Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="定时索引设置" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" 
                   Text="请选择索引执行的时间间隔：" 
                   Margin="0,0,0,5"/>
        
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ComboBox x:Name="IntervalComboBox"
                      Grid.Column="0"
                      Width="200"
                      SelectionChanged="IntervalComboBox_SelectionChanged"/>
            
            <Grid Grid.Column="1" Margin="10,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="CustomIntervalTextBox"
                         Grid.Column="0"
                         Width="60"
                         TextChanged="CustomIntervalTextBox_TextChanged"/>
                <TextBlock Grid.Column="1"
                           Text="分钟" 
                           VerticalAlignment="Center" 
                           Margin="5,0,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</Window>